# Apple Notarization Credentials
# ==============================
# These credentials are used by xcrun notarytool to submit binaries to Apple for notarization.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.local.example .env.local
# 2. Fill in your Apple Developer credentials below
# 3. Run the one-time keychain setup (see docs/macos-distribution.md)
#
# IMPORTANT: Never commit .env.local to git! It contains sensitive credentials.

# Your Apple ID email (the one used for Apple Developer account)
APPLE_ID=your-apple-id@example.com

# Team ID from your Developer ID certificate
# Found via: security find-identity -v -p codesigning | grep "Developer ID"
APPLE_TEAM_ID=YOUR_TEAM_ID

# App-specific password generated at https://appleid.apple.com
# (Sign In -> App-Specific Passwords -> Generate)
NOTARYTOOL_PASSWORD=xxxx-xxxx-xxxx-xxxx
